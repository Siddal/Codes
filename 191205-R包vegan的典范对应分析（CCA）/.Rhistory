plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺', display = c('wa', 'sp', 'cn'))
plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺', display = c('sp'))
plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺', display = c('cn'))
plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺', display = c('cn'))
# Summary of otu and digestion CCA result
otuDigestion_cca <- cca(otu, digestion)
plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺 (species scores)', display = c('sp'))
plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺 (site scores)', display = c('wa'))
plot(otuDigestion_cca, scaling = 2, main = 'II 型标尺', display = c('wa', 'sp', 'cn'))
# Summary of otu and digestion CCA result
otuDigestion_cca <- cca(otu, digestion)
plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺 (species scores)', display = c('sp','cn'))
plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺 (site scores)', display = c('wa', 'cn'))
plot(otuDigestion_cca, scaling = 2, main = 'II 型标尺', display = c('wa', 'sp', 'cn'))
# Summary of otu and digestion CCA result
otuDigestion_cca <- cca(otu, digestion)
plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺 (species scores)', display = c('sp','cn'))
plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺 (site scores)', display = c('wa', 'cn'))
plot(otuDigestion_cca, scaling = 2, main = 'II 型标尺 (species scores)', display = c('sp', 'cn'))
plot(otuDigestion_cca, scaling = 2, main = 'II 型标尺 (site scores)', display = c('wa', 'cn'))
# Summary of otu and digestion CCA result
otuDigestion_cca <- cca(otu, digestion)
plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺 (species scores)', display = c('sp','cn'))
plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺 (site scores)', display = c('wa', 'cn'))
plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺 (All scores)', display = c('wa', 'sp', 'cn'))
plot(otuDigestion_cca, scaling = 2, main = 'II 型标尺 (species scores)', display = c('sp', 'cn'))
plot(otuDigestion_cca, scaling = 2, main = 'II 型标尺 (site scores)', display = c('wa', 'cn'))
plot(otuDigestion_cca, scaling = 2, main = 'II 型标尺 (All scores)', display = c('wa', 'sp', 'cn'))
# Summary of otu and digestion CCA result
otuDigestion_cca <- cca(otu, digestion)
# plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺 (species scores)', display = c('sp','cn'))
# plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺 (site scores)', display = c('wa', 'cn'))
plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺 (All scores)', display = c('wa', 'sp', 'cn'))
# plot(otuDigestion_cca, scaling = 2, main = 'II 型标尺 (species scores)', display = c('sp', 'cn'))
# plot(otuDigestion_cca, scaling = 2, main = 'II 型标尺 (site scores)', display = c('wa', 'cn'))
plot(otuDigestion_cca, scaling = 2, main = 'II 型标尺 (All scores)', display = c('wa', 'sp', 'cn'))
plot(otu_cca, scaling = 1, main = 'I 型标尺，加权', display = c('wa', 'cn'))
plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺，加权', display = c('wa', 'cn'))
plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺，拟合', display = c('lc', 'cn'))
# Summary of otu and digestion CCA result
otuDigestion_cca <- cca(otu, digestion)
# plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺 (species scores)', display = c('sp','cn'))
# plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺 (site scores)', display = c('wa', 'cn'))
plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺 (All scores)', display = c('wa', 'sp', 'cn'))
# plot(otuDigestion_cca, scaling = 2, main = 'II 型标尺 (species scores)', display = c('sp', 'cn'))
# plot(otuDigestion_cca, scaling = 2, main = 'II 型标尺 (site scores)', display = c('wa', 'cn'))
plot(otuDigestion_cca, scaling = 2, main = 'II 型标尺 (All scores)', display = c('wa', 'sp', 'cn', 'ln'))
plot(otuDigestion_cca, scaling = 2, main = 'II 型标尺 (All scores)', display = c('wa', 'sp', 'cn'))
# Summary of otu and digestion CCA result
otuDigestion_cca <- cca(otu, digestion)
# plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺 (species scores)', display = c('sp','cn'))
# plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺 (site scores)', display = c('wa', 'cn'))
plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺 (All scores)', display = c('wa', 'sp', 'cn'))
# plot(otuDigestion_cca, scaling = 2, main = 'II 型标尺 (species scores)', display = c('sp', 'cn'))
# plot(otuDigestion_cca, scaling = 2, main = 'II 型标尺 (site scores)', display = c('wa', 'cn'))
plot(otuDigestion_cca, scaling = 2, main = 'II 型标尺 (All scores)', display = c('wa', 'sp', 'cn'))
plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺，加权', display = c('wa', 'cn'))
plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺，加权', display = c('wa', 'cn', 'sp'))
plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺，拟合', display = c('lc', 'cn'))
plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺，加权', display = c('wa', 'cn'))
plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺，加权', display = c('wa', 'cn', 'sp'))
plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺，拟合', display = c('lc', 'cn'))
plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺，拟合', display = c('lc', 'cn', 'sp'))
# Summary of otu and digestion CCA result
otuDigestion_cca <- cca(otu, digestion)
# plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺 (species scores)', display = c('sp','cn'))
# plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺 (site scores)', display = c('wa', 'cn'))
plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺，加权 (All scores)', display = c('wa', 'sp', 'cn'))
# plot(otuDigestion_cca, scaling = 2, main = 'II 型标尺 (species scores)', display = c('sp', 'cn'))
# plot(otuDigestion_cca, scaling = 2, main = 'II 型标尺 (site scores)', display = c('wa', 'cn'))
plot(otuDigestion_cca, scaling = 2, main = 'II 型标尺，加权 (All scores)', display = c('wa', 'sp', 'cn'))
plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺，拟合', display = c('lc', 'cn'))
plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺，拟合', display = c('lc', 'cn', 'sp'))
plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺，拟合(site scores)', display = c('lc', 'cn'))
plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺，拟合', display = c('lc', 'cn', 'sp'))
plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺，拟合(site scores)', display = c('lc', 'cn'))
plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺，拟合(All scores)', display = c('lc', 'cn', 'sp'))
plot(otuDigestion_cca, scaling = 2, main = 'II 型标尺，拟合(site scores)', display = c('lc', 'cn'))
plot(otuDigestion_cca, scaling = 2, main = 'II 型标尺，拟合(All scores)', display = c('lc', 'cn', 'sp'))
# Summary of otu and digestion CCA result
otuDigestion_cca <- cca(otu, digestion)
# plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺，加权 (species scores)', display = c('sp','cn'))
# plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺，加权 (site scores)', display = c('wa', 'cn'))
plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺，加权 (all scores)', display = c('wa', 'sp', 'cn'))
# plot(otuDigestion_cca, scaling = 2, main = 'II 型标尺，加权 (species scores)', display = c('sp', 'cn'))
# plot(otuDigestion_cca, scaling = 2, main = 'II 型标尺，加权 (site scores)', display = c('wa', 'cn'))
plot(otuDigestion_cca, scaling = 2, main = 'II 型标尺，加权 (all scores)', display = c('wa', 'sp', 'cn'))
plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺，拟合(site scores)', display = c('lc', 'cn'))
plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺，拟合(all scores)', display = c('lc', 'cn', 'sp'))
plot(otuDigestion_cca, scaling = 2, main = 'II 型标尺，拟合(site scores)', display = c('lc', 'cn'))
plot(otuDigestion_cca, scaling = 2, main = 'II 型标尺，拟合(all scores)', display = c('lc', 'cn', 'sp'))
# Summary of otu and digestion CCA result
otuDigestion_cca <- cca(otu, digestion)
# plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺，加权 (species scores)', display = c('sp','cn'))
# plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺，加权 (site scores)', display = c('wa', 'cn'))
plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺，加权 (all scores)', display = c('wa', 'sp', 'cn'))
# plot(otuDigestion_cca, scaling = 2, main = 'II 型标尺，加权 (species scores)', display = c('sp', 'cn'))
# plot(otuDigestion_cca, scaling = 2, main = 'II 型标尺，加权 (site scores)', display = c('wa', 'cn'))
plot(otuDigestion_cca, scaling = 2, main = 'II 型标尺，加权 (all scores)', display = c('wa', 'sp', 'cn'))
plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺，拟合(site scores)', display = c('lc', 'cn'))
plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺，拟合(all scores)', display = c('lc', 'cn', 'sp'))
plot(otuDigestion_cca, scaling = 2, main = 'II 型标尺，拟合(site scores)', display = c('lc', 'cn'))
plot(otuDigestion_cca, scaling = 2, main = 'II 型标尺，拟合(all scores)', display = c('lc', 'cn', 'sp'))
r2 <- RsquareAdj(otuDigestion_cca)
otuDigestion_cca_noadj <- r2$r.squared       #original R2
otuDigestion_cca_adj <- r2$adj.r.squared     #adjusted R2
otuDigestion_cca_noadj
otuDigestion_cca_adj
print(otuDigestion_cca_noadj)
a <- 1
a
a
r2 <- RsquareAdj(otuDigestion_cca)
otuDigestion_cca_noadj <- r2$r.squared       #original R2
otuDigestion_cca_adj <- r2$adj.r.squared     #adjusted R2
a <- 1
a
otuDigestion_cca_noadj
otuDigestion_cca_adj
otuDigestion_cca_noadj
otuDigestion_cca_adj
a <- 1
a
print(a)
r2
# Summary of otu and digestion CCA result
otuDigestion_cca <- cca(otu, digestion)
# plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺，加权 (species scores)', display = c('sp','cn'))
# plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺，加权 (site scores)', display = c('wa', 'cn'))
plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺，加权 (all scores)', display = c('wa', 'sp', 'cn'))
# plot(otuDigestion_cca, scaling = 2, main = 'II 型标尺，加权 (species scores)', display = c('sp', 'cn'))
# plot(otuDigestion_cca, scaling = 2, main = 'II 型标尺，加权 (site scores)', display = c('wa', 'cn'))
plot(otuDigestion_cca, scaling = 2, main = 'II 型标尺，加权 (all scores)', display = c('wa', 'sp', 'cn'))
plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺，拟合(site scores)', display = c('lc', 'cn'))
plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺，拟合(all scores)', display = c('lc', 'cn', 'sp'))
plot(otuDigestion_cca, scaling = 2, main = 'II 型标尺，拟合(site scores)', display = c('lc', 'cn'))
plot(otuDigestion_cca, scaling = 2, main = 'II 型标尺，拟合(all scores)', display = c('lc', 'cn', 'sp'))
r2 <- RsquareAdj(otuDigestion_cca)
otuDigestion_cca_noadj <- r2$r.squared       #original R2
otuDigestion_cca_adj <- r2$adj.r.squared     #adjusted R2
otuDigestion_cca_noadj
otuDigestion_cca_adj
a
print("afdasfasd")
# r2 <- RsquareAdj(otuDigestion_cca)
# otuDigestion_cca_noadj <- r2$r.squared       #original R2
# otuDigestion_cca_adj <- r2$adj.r.squared     #adjusted R2
#
# otuDigestion_cca_noadj
# otuDigestion_cca_adj
print("afdasfasd")
# Summary of otu and digestion CCA result
otuDigestion_cca <- cca(otu, digestion)
# plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺，加权 (species scores)', display = c('sp','cn'))
# plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺，加权 (site scores)', display = c('wa', 'cn'))
plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺，加权 (all scores)', display = c('wa', 'sp', 'cn'))
# plot(otuDigestion_cca, scaling = 2, main = 'II 型标尺，加权 (species scores)', display = c('sp', 'cn'))
# plot(otuDigestion_cca, scaling = 2, main = 'II 型标尺，加权 (site scores)', display = c('wa', 'cn'))
plot(otuDigestion_cca, scaling = 2, main = 'II 型标尺，加权 (all scores)', display = c('wa', 'sp', 'cn'))
plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺，拟合(site scores)', display = c('lc', 'cn'))
plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺，拟合(all scores)', display = c('lc', 'cn', 'sp'))
plot(otuDigestion_cca, scaling = 2, main = 'II 型标尺，拟合(site scores)', display = c('lc', 'cn'))
plot(otuDigestion_cca, scaling = 2, main = 'II 型标尺，拟合(all scores)', display = c('lc', 'cn', 'sp'))
print("afdasfasd")
defaultdir <- getwd()
##è¯»åæ°æ®
#è¯»å¥ç©ç§æ°æ®ï¼ä»¥ç»è OTU æ°´å¹³ä¸°åº¦è¡¨ä¸ºä¾
otu <- read.delim(paste(defaultdir,"/","otu_table.txt",sep=""), row.names = 1, sep = '\t', stringsAsFactors = FALSE, check.names = FALSE)
otu <- data.frame(t(otu))
#è¯»åç¯å¢æ°æ®
env <- read.delim(paste(defaultdir,"/","env_table.txt",sep=""), row.names = 1, sep = '\t', stringsAsFactors = FALSE, check.names = FALSE)
#æèæ ¼å¼ 2ï¼cca(Y~var1+var2+var3+factorA+var2*var3+Condition(var4))
#var1ãvar2 ç�ï¼æ°å¼åè§£éåéï¼factorAï¼å å�åè§£éåéï¼var2*var3ï¼èèåéé´çäº¤äºä½ç¨ï¼Condition(var4)ï¼åé 4 ä½ä¸ºååé
#Y~. æ¯ Y~var1+var2+...+varn çç®åï¼ä¸æ¶åäº¤äºä½ç¨åååé
otu_cca <- cca(otu~., env)
library(vegan)
#æèæ ¼å¼ 2ï¼cca(Y~var1+var2+var3+factorA+var2*var3+Condition(var4))
#var1ãvar2 ç�ï¼æ°å¼åè§£éåéï¼factorAï¼å å�åè§£éåéï¼var2*var3ï¼èèåéé´çäº¤äºä½ç¨ï¼Condition(var4)ï¼åé 4 ä½ä¸ºååé
#Y~. æ¯ Y~var1+var2+...+varn çç®åï¼ä¸æ¶åäº¤äºä½ç¨åååé
otu_cca <- cca(otu~., env)
#ååéæ©ï¼è¿éä»¥ ordiR2step() çæ¹æ³ä¸ºä¾ï¼åºäº 999 æ¬¡ç½®æ¢æ£éªï¼è¯¦æ ?ordiR2step
otu_cca_forward_pr <- ordiR2step(cca(otu~1, env), scope = formula(otu_cca), R2scope = TRUE, direction = 'forward', permutations = 999)
View(otu_cca)
# setwd(paste(defaultdir,"/graph",sep=""))
#Adding the required packages for this file. Don't change the current order, and adding new packages to the first.
packages <- c(
"ggplot2",
"tidyverse",
"stringr",
"dplyr",      # for pipe (%>%) command
"factoextra",
"vegan",
"CCA"
)
if (length(setdiff(packages, rownames(installed.packages()))) > 0) {
install.packages(setdiff(packages, rownames(installed.packages())))
lapply(packages, library, character.only = TRUE)
} else {
lapply(packages, library, character.only = TRUE)
}
remove(packages)
# update.packages(ask = FALSE, checkBuilt = TRUE)
defaultdir <- getwd()
dir.create("graph")
# paste(defaultdir,"/graph/","name",sep="")
dir.create("table")
# paste(defaultdir,"/table/","name",sep="")
dir.create("pdf")
# paste(defaultdir,"/pdf/","name.pdf",sep="")
dir.create("outputs")
# paste(defaultdir,"/outputs/","name",sep="")
setwd(defaultdir)
otu <- read.csv(
paste(defaultdir,"/data/","otu.csv",sep=""),
header = T, sep = ",")
rownames(otu) <- otu$OTU_Id
otu <- otu[, -c(1, 42:ncol(otu))]
otu <- data.frame(t(otu))
metadata <- read.csv(
paste(defaultdir,"/data/","metadata.csv",sep=""),
header = T, sep = ",")
rownames(metadata) <- metadata$SampleId
digestion <- read.csv(
paste(defaultdir,"/data/","DigestionNutrient.csv",sep=""),
header = T, sep = ",")
rownames(digestion) <- digestion$SampleId
digestion <- digestion[,-1]
leaching <- read.csv(
paste(defaultdir,"/data/","LeachingNutrient.csv",sep=""),
header = T, sep = ",")
rownames(leaching) <- leaching$SampleId
leaching <- leaching[,-1]
leaching <- leaching[,-c(19:ncol(leaching))] #delete NA nutrients
# otuLeaching_cca <- cca(otu, leaching)
# summary(otuLeaching_cca, scaling = 1)
# summary(otuDigestion_cca, scaling = 1)
# Summary of otu and digestion CCA result
otuDigestion_cca <- cca(otu~., digestion)
# plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺，加权 (species scores)', display = c('sp','cn'))
# plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺，加权 (site scores)', display = c('wa', 'cn'))
plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺，加权 (all scores)', display = c('wa', 'sp', 'cn'))
# plot(otuDigestion_cca, scaling = 2, main = 'II 型标尺，加权 (species scores)', display = c('sp', 'cn'))
# plot(otuDigestion_cca, scaling = 2, main = 'II 型标尺，加权 (site scores)', display = c('wa', 'cn'))
plot(otuDigestion_cca, scaling = 2, main = 'II 型标尺，加权 (all scores)', display = c('wa', 'sp', 'cn'))
plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺，拟合(site scores)', display = c('lc', 'cn'))
plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺，拟合(all scores)', display = c('lc', 'cn', 'sp'))
plot(otuDigestion_cca, scaling = 2, main = 'II 型标尺，拟合(site scores)', display = c('lc', 'cn'))
plot(otuDigestion_cca, scaling = 2, main = 'II 型标尺，拟合(all scores)', display = c('lc', 'cn', 'sp'))
vif.cca(otuDigestion_cca)
otu_cca_forward_pr <- ordiR2step(cca(otu~1, digestion), scope = formula(otuDigestion_cca), R2scope = TRUE, direction = 'forward', permutations = 999)
# otu_cca_forward_pr <- ordiR2step(cca(otu~1, env), scope = formula(otu_cca), R2scope = TRUE, direction = 'forward', permutations = 999)
# setwd(paste(defaultdir,"/graph",sep=""))
#Adding the required packages for this file. Don't change the current order, and adding new packages to the first.
packages <- c(
"ggplot2",
"tidyverse",
"stringr",
"dplyr",      # for pipe (%>%) command
"factoextra",
"vegan",
"CCA"
)
if (length(setdiff(packages, rownames(installed.packages()))) > 0) {
install.packages(setdiff(packages, rownames(installed.packages())))
lapply(packages, library, character.only = TRUE)
} else {
lapply(packages, library, character.only = TRUE)
}
remove(packages)
# update.packages(ask = FALSE, checkBuilt = TRUE)
defaultdir <- getwd()
dir.create("graph")
# paste(defaultdir,"/graph/","name",sep="")
dir.create("table")
# paste(defaultdir,"/table/","name",sep="")
dir.create("pdf")
# paste(defaultdir,"/pdf/","name.pdf",sep="")
dir.create("outputs")
# paste(defaultdir,"/outputs/","name",sep="")
setwd(defaultdir)
otu <- read.csv(
paste(defaultdir,"/data/","otu.csv",sep=""),
header = T, sep = ",")
rownames(otu) <- otu$OTU_Id
otu <- otu[, -c(1, 42:ncol(otu))]
otu <- data.frame(t(otu))
metadata <- read.csv(
paste(defaultdir,"/data/","metadata.csv",sep=""),
header = T, sep = ",")
rownames(metadata) <- metadata$SampleId
digestion <- read.csv(
paste(defaultdir,"/data/","DigestionNutrient.csv",sep=""),
header = T, sep = ",")
rownames(digestion) <- digestion$SampleId
digestion <- digestion[,-1]
leaching <- read.csv(
paste(defaultdir,"/data/","LeachingNutrient.csv",sep=""),
header = T, sep = ",")
rownames(leaching) <- leaching$SampleId
leaching <- leaching[,-1]
leaching <- leaching[,-c(19:ncol(leaching))] #delete NA nutrients
# otuLeaching_cca <- cca(otu, leaching)
# summary(otuLeaching_cca, scaling = 1)
# summary(otuDigestion_cca, scaling = 1)
# Summary of otu and digestion CCA result
otuDigestion_cca <- cca(otu~., digestion)
# plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺，加权 (species scores)', display = c('sp','cn'))
# plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺，加权 (site scores)', display = c('wa', 'cn'))
plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺，加权 (all scores)', display = c('wa', 'sp', 'cn'))
# plot(otuDigestion_cca, scaling = 2, main = 'II 型标尺，加权 (species scores)', display = c('sp', 'cn'))
# plot(otuDigestion_cca, scaling = 2, main = 'II 型标尺，加权 (site scores)', display = c('wa', 'cn'))
plot(otuDigestion_cca, scaling = 2, main = 'II 型标尺，加权 (all scores)', display = c('wa', 'sp', 'cn'))
plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺，拟合(site scores)', display = c('lc', 'cn'))
plot(otuDigestion_cca, scaling = 1, main = 'I 型标尺，拟合(all scores)', display = c('lc', 'cn', 'sp'))
plot(otuDigestion_cca, scaling = 2, main = 'II 型标尺，拟合(site scores)', display = c('lc', 'cn'))
plot(otuDigestion_cca, scaling = 2, main = 'II 型标尺，拟合(all scores)', display = c('lc', 'cn', 'sp'))
print(paste("The original R^2 for digestion is ", otuDigestion_cca_noadj, "The adjusted R^2 for digestion is ", otuDigestion_cca_adj, sep = ""))
r2 <- RsquareAdj(otuDigestion_cca)
otuDigestion_cca_noadj <- r2$r.squared       #original R2
otuDigestion_cca_adj <- r2$adj.r.squared     #adjusted R2
print(paste("The original R^2 for digestion is ", otuDigestion_cca_noadj, "The adjusted R^2 for digestion is ", otuDigestion_cca_adj, sep = ""))
print(paste("The original R^2 for digestion is ", otuDigestion_cca_noadj, "/nThe adjusted R^2 for digestion is ", otuDigestion_cca_adj, sep = ""))
print(paste("The original R^2 for digestion is ", otuDigestion_cca_noadj, "The adjusted R^2 for digestion is ", otuDigestion_cca_adj, sep = "\n))
print(paste("The original R^2 for digestion is ", otuDigestion_cca_noadj, "The adjusted R^2 for digestion is ", otuDigestion_cca_adj, sep = "\n"))
print(paste("The original R^2 for digestion is ", otuDigestion_cca_noadj, "The adjusted R^2 for digestion is ", otuDigestion_cca_adj, sep = ""))
plot(otu_cca_forward_pr, scaling = 1, main = '前向选择后，I 型标尺', display = c('wa', 'cn'))
plot(otuDigestion_cca_forward_pr, scaling = 1, main = '前向选择后，I 型标尺', display = c('wa', 'cn'))
vif.cca(otuDigestion_cca)
otuDigestion_cca_forward_pr <- ordiR2step(cca(otu~1, digestion), scope = formula(otuDigestion_cca), R2scope = TRUE, direction = 'forward', permutations = 999)
# plot(otuDigestion_cca_forward_pr, scaling = 1, main = '前向选择后，I 型标尺', display = c('wa', 'cn'))
plot(otuDigestion_cca_forward_pr, scaling = 1, main = '前向选择后，I 型标尺', display = c('wa', 'cn'))
plot(otuDigestion_cca_forward_pr, scaling = 1, main = '前向选择后，I 型标尺', display = c('wa', 'sp', 'cn'))
otuDigestion_cca_forward_pr
summary(otuDigestion_cca_forward_pr)
summary(otuDigestion_cca_forward_pr, scaling = 1)
plot(otuDigestion_cca_forward_pr, scaling = 1, main = '前向选择后，I 型标尺', display = c('sp', 'cn'))
plot(otuDigestion_cca_forward_pr, scaling = 1, main = '前向选择后，I 型标尺', display = c('wa', 'cn'))
plot(otuDigestion_cca_forward_pr, scaling = 1, main = '前向选择后，I 型标尺', display = c('wa', 'cn'))
plot(otuDigestion_cca_forward_pr, scaling = 2, main = '前向选择后，II 型标尺', display = c('wa', 'cn'))
plot(otuDigestion_cca_forward_pr, scaling = 1, main = '前向选择后，I 型标尺 (site scores)', display = c('wa', 'cn'))
plot(otuDigestion_cca_forward_pr, scaling = 2, main = '前向选择后，II 型标尺 (site scores)', display = c('wa', 'cn'))
plot(otuDigestion_cca_forward_pr, scaling = 1, main = '前向选择后，I 型标尺 (species scores)', display = c('sp', 'cn'))
plot(otuDigestion_cca_forward_pr, scaling = 2, main = '前向选择后，II 型标尺 (species scores)', display = c('sp', 'cn'))
plot(otuDigestion_cca_forward_pr, scaling = 1, main = '前向选择后，I 型标尺 (all scores)', display = c('wa', 'sp', 'cn'))
plot(otuDigestion_cca_forward_pr, scaling = 2, main = '前向选择后，II 型标尺 (all scores)', display = c('wa', 'sp', 'cn'))
View(digestion)
# setwd(paste(defaultdir,"/graph",sep=""))
#Adding the required packages for this file. Don't change the current order, and adding new packages to the first.
packages <- c(
"ggplot2",
"tidyverse",
"stringr",
"dplyr",      # for pipe (%>%) command
"factoextra",
"vegan",
"CCA"
)
if (length(setdiff(packages, rownames(installed.packages()))) > 0) {
install.packages(setdiff(packages, rownames(installed.packages())))
lapply(packages, library, character.only = TRUE)
} else {
lapply(packages, library, character.only = TRUE)
}
remove(packages)
# update.packages(ask = FALSE, checkBuilt = TRUE)
defaultdir <- getwd()
dir.create("graph")
# paste(defaultdir,"/graph/","name",sep="")
dir.create("table")
# paste(defaultdir,"/table/","name",sep="")
dir.create("pdf")
# paste(defaultdir,"/pdf/","name.pdf",sep="")
dir.create("outputs")
# paste(defaultdir,"/outputs/","name",sep="")
setwd(defaultdir)
otu <- read.csv(
paste(defaultdir,"/data/","otu.csv",sep=""),
header = T, sep = ",")
rownames(otu) <- otu$OTU_Id
otu <- otu[, -c(1, 42:ncol(otu))]
otu <- data.frame(t(otu))
metadata <- read.csv(
paste(defaultdir,"/data/","metadata.csv",sep=""),
header = T, sep = ",")
rownames(metadata) <- metadata$SampleId
digestion <- read.csv(
paste(defaultdir,"/data/","DigestionNutrient.csv",sep=""),
header = T, sep = ",")
rownames(digestion) <- digestion$SampleId
digestion <- digestion[,-1]
leaching <- read.csv(
paste(defaultdir,"/data/","LeachingNutrient.csv",sep=""),
header = T, sep = ",")
rownames(leaching) <- leaching$SampleId
leaching <- leaching[,-1]
leaching <- leaching[,-c(19:ncol(leaching))] #delete NA nutrients
# otuLeaching_cca <- cca(otu, leaching)
# summary(otuLeaching_cca, scaling = 1)
# summary(otuDigestion_cca, scaling = 1)
# Summary of otu and leaching CCA result
otuLeaching_cca <- cca(otu~., leaching)
# plot(otuLeaching_cca, scaling = 1, main = 'I-type，weighted (species scores)', display = c('sp','cn'))
# plot(otuLeaching_cca, scaling = 1, main = 'I-type, weighted (site scores)', display = c('wa', 'cn'))
plot(otuLeaching_cca, scaling = 1, main = 'I-type, weighted (all scores for leaching)', display = c('wa', 'sp', 'cn'))
# plot(otuLeaching_cca, scaling = 2, main = 'II-type, weighted (species scores)', display = c('sp', 'cn'))
# plot(otuLeaching_cca, scaling = 2, main = 'II-type, weighted (site scores)', display = c('wa', 'cn'))
plot(otuLeaching_cca, scaling = 2, main = 'II-type, weighted (all scores for leaching)', display = c('wa', 'sp', 'cn'))
plot(otuLeaching_cca, scaling = 1, main = 'I-type，fit (site scores for leaching)', display = c('lc', 'cn'))
plot(otuLeaching_cca, scaling = 1, main = 'I-type, fit (all scores for leaching)', display = c('lc', 'cn', 'sp'))
plot(otuLeaching_cca, scaling = 2, main = 'II-type, fit (site scores for leaching)', display = c('lc', 'cn'))
plot(otuLeaching_cca, scaling = 2, main = 'II-type, fit (all scores for leaching)', display = c('lc', 'cn', 'sp'))
# plot(otuLeaching_cca, scaling = 1, main = 'I-type，weighted (species scores)', display = c('sp','cn'))
# plot(otuLeaching_cca, scaling = 1, main = 'I-type, weighted (site scores)', display = c('wa', 'cn'))
plot(otuLeaching_cca, scaling = 1, main = 'I-type, weighted (all scores for leaching)', display = c('wa', 'sp'))
# Summary of otu and leaching CCA result
otuLeaching_cca <- cca(otu~., leaching)
# plot(otuLeaching_cca, scaling = 1, main = 'I-type，weighted (species scores)', display = c('sp','cn'))
# plot(otuLeaching_cca, scaling = 1, main = 'I-type, weighted (site scores)', display = c('wa', 'cn'))
plot(otuLeaching_cca, scaling = 1, main = 'I-type, weighted (all scores for leaching)', display = c('wa', 'sp', , 'cn'))
# Summary of otu and leaching CCA result
otuLeaching_cca <- cca(otu~., leaching)
# plot(otuLeaching_cca, scaling = 1, main = 'I-type，weighted (species scores)', display = c('sp','cn'))
# plot(otuLeaching_cca, scaling = 1, main = 'I-type, weighted (site scores)', display = c('wa', 'cn'))
plot(otuLeaching_cca, scaling = 1, main = 'I-type, weighted (all scores for leaching)', display = c('wa', 'sp', 'cn'))
# plot(otuLeaching_cca, scaling = 2, main = 'II-type, weighted (species scores)', display = c('sp', 'cn'))
# plot(otuLeaching_cca, scaling = 2, main = 'II-type, weighted (site scores)', display = c('wa', 'cn'))
plot(otuLeaching_cca, scaling = 2, main = 'II-type, weighted (all scores for leaching)', display = c('wa', 'sp', 'cn'))
plot(otuLeaching_cca, scaling = 1, main = 'I-type，fit (site scores for leaching)', display = c('lc', 'cn'))
plot(otuLeaching_cca, scaling = 1, main = 'I-type, fit (all scores for leaching)', display = c('lc', 'cn', 'sp'))
plot(otuLeaching_cca, scaling = 2, main = 'II-type, fit (site scores for leaching)', display = c('lc', 'cn'))
plot(otuLeaching_cca, scaling = 2, main = 'II-type, fit (all scores for leaching)', display = c('lc', 'cn', 'sp'))
vif.cca(otuLeaching_cca)
otuLeaching_cca_forward_pr <- ordiR2step(cca(otu~1, leaching), scope = formula(otuLeaching_cca), R2scope = TRUE, direction = 'forward', permutations = 999)
plot(otuLeaching_cca_forward_pr, scaling = 1, main = 'forward selection, I-type (site scores for leaching)', display = c('wa', 'cn'))
plot(otuLeaching_cca_forward_pr, scaling = 2, main = 'forward selection, II-type (site scores for leaching)', display = c('wa', 'cn'))
plot(otuLeaching_cca_forward_pr, scaling = 1, main = 'forward selection, I-type (species scores for leaching)', display = c('sp', 'cn'))
plot(otuLeaching_cca_forward_pr, scaling = 2, main = 'forward selection, I-type (species scores for leaching)', display = c('sp', 'cn'))
plot(otuLeaching_cca_forward_pr, scaling = 1, main = 'forward selection, I-type (all scores for leaching)', display = c('wa', 'sp', 'cn'))
plot(otuLeaching_cca_forward_pr, scaling = 2, main = 'forward selection, II-type (all scores for leaching)', display = c('wa', 'sp', 'cn'))
# setwd(paste(defaultdir,"/graph",sep=""))
#Adding the required packages for this file. Don't change the current order, and adding new packages to the first.
packages <- c(
"ggplot2",
"tidyverse",
"stringr",
"dplyr",      # for pipe (%>%) command
"factoextra",
"vegan",
"CCA"
)
if (length(setdiff(packages, rownames(installed.packages()))) > 0) {
install.packages(setdiff(packages, rownames(installed.packages())))
lapply(packages, library, character.only = TRUE)
} else {
lapply(packages, library, character.only = TRUE)
}
remove(packages)
# update.packages(ask = FALSE, checkBuilt = TRUE)
defaultdir <- getwd()
dir.create("graph")
# paste(defaultdir,"/graph/","name",sep="")
dir.create("table")
# paste(defaultdir,"/table/","name",sep="")
dir.create("pdf")
# paste(defaultdir,"/pdf/","name.pdf",sep="")
dir.create("outputs")
# paste(defaultdir,"/outputs/","name",sep="")
setwd(defaultdir)
otu <- read.csv(
paste(defaultdir,"/data/","otu.csv",sep=""),
header = T, sep = ",")
rownames(otu) <- otu$OTU_Id
otu <- otu[, -c(1, 42:ncol(otu))]
otu <- data.frame(t(otu))
metadata <- read.csv(
paste(defaultdir,"/data/","metadata.csv",sep=""),
header = T, sep = ",")
rownames(metadata) <- metadata$SampleId
digestion <- read.csv(
paste(defaultdir,"/data/","DigestionNutrient.csv",sep=""),
header = T, sep = ",")
rownames(digestion) <- digestion$SampleId
digestion <- digestion[,-1]
leaching <- read.csv(
paste(defaultdir,"/data/","LeachingNutrient.csv",sep=""),
header = T, sep = ",")
rownames(leaching) <- leaching$SampleId
leaching <- leaching[,-1]
leaching <- leaching[,-c(19:ncol(leaching))] #delete NA nutrients
# otuLeaching_cca <- cca(otu, leaching)
# summary(otuLeaching_cca, scaling = 1)
# summary(otuDigestion_cca, scaling = 1)
# Summary of otu and digestion CCA result
otuDigestion_cca <- cca(otu~., digestion)
# plot(otuDigestion_cca, scaling = 1, main = 'I-type，weighted (species scores)', display = c('sp','cn'))
# plot(otuDigestion_cca, scaling = 1, main = 'I-type, weighted (site scores)', display = c('wa', 'cn'))
plot(otuDigestion_cca, scaling = 1, main = 'I-type, weighted (all scores for digestion)', display = c('wa', 'sp', 'cn'))
# plot(otuDigestion_cca, scaling = 2, main = 'II-type, weighted (species scores)', display = c('sp', 'cn'))
# plot(otuDigestion_cca, scaling = 2, main = 'II-type, weighted (site scores)', display = c('wa', 'cn'))
plot(otuDigestion_cca, scaling = 2, main = 'II-type, weighted (all scores for digestion)', display = c('wa', 'sp', 'cn'))
plot(otuDigestion_cca, scaling = 1, main = 'I-type，fit (site scores for digestion)', display = c('lc', 'cn'))
plot(otuDigestion_cca, scaling = 1, main = 'I-type, fit (all scores for digestion)', display = c('lc', 'cn', 'sp'))
plot(otuDigestion_cca, scaling = 2, main = 'II-type, fit (site scores for digestion)', display = c('lc', 'cn'))
plot(otuDigestion_cca, scaling = 2, main = 'II-type, fit (all scores for digestion)', display = c('lc', 'cn', 'sp'))
vif.cca(otuDigestion_cca)
otuDigestion_cca_forward_pr <- ordiR2step(cca(otu~1, digestion), scope = formula(otuDigestion_cca), R2scope = TRUE, direction = 'forward', permutations = 999)
plot(otuDigestion_cca_forward_pr, scaling = 1, main = 'forward selection, I-type (site scores for digestion)', display = c('wa', 'cn'))
plot(otuDigestion_cca_forward_pr, scaling = 2, main = 'forward selection, II-type (site scores for digestion)', display = c('wa', 'cn'))
plot(otuDigestion_cca_forward_pr, scaling = 1, main = 'forward selection, I-type (species scores for digestion)', display = c('sp', 'cn'))
plot(otuDigestion_cca_forward_pr, scaling = 2, main = 'forward selection, I-type (species scores for digestion)', display = c('sp', 'cn'))
plot(otuDigestion_cca_forward_pr, scaling = 1, main = 'forward selection, I-type (all scores for digestion)', display = c('wa', 'sp', 'cn'))
plot(otuDigestion_cca_forward_pr, scaling = 2, main = 'forward selection, II-type (all scores for digestion)', display = c('wa', 'sp', 'cn'))
